apiVersion: v1
kind: Service
metadata:
  name: udagram-frontent-app
  labels:
    run: udagram-frontent-app
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: udagram-frontent-app